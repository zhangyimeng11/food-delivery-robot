server:
  host: 0.0.0.0
  port: 8765

# LLM 配置（用于智能解析搜索结果）
llm:
  api_key: "sk-8ca63b6b547c429ba348eeb131ae1bd0"
  base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
  model: "qwen-flash"

# 通知监听配置
notification:
  enabled: true
  check_interval: 3  # 秒
  keywords:
    - "外卖已送达"
    - "订单已完成"
    - "骑手已送达"
    - "已送达"
    - "请取餐"

# 机器人通知（可选）
robot:
  enabled: false
  api_url: http://localhost:8000/robot/notify
  api_key: ""

# 平台通知（可选）
platform:
  enabled: false
  webhook_url: http://your-platform.com/webhook
  api_key: ""

