# simple-voice-client 开发环境 Dockerfile
FROM node:20-alpine

WORKDIR /app

# 复制依赖文件
COPY package.json package-lock.json ./

# 安装依赖
RUN npm ci

# 暴露 Vite 开发服务器端口
EXPOSE 3001

# 开发模式：源代码会通过 volume 挂载
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
